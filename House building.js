let available = 5
let price = 10
let HousesUsed = 0
let placed1 = false
let placed2 = false
let placed3 = false
let placed4 = false
let placed5 = false

function draw() {
    if (placed1 = true && HousesUsed < 1) {
        rd.add('placednow', () => available - 5 && HousesUsed + 1)
        
    }
}

function draw() {
    if (placed2 = true && HousesUsed < 2) {
        rd.add('placednow', () => available - 5 && HousesUsed + 1)
        
    }
}

function draw() {
    if (placed1 = true && HousesUsed < 3) {
        rd.add('placednow', () => available - 5 && HousesUsed + 1)
        
    }
}

function draw() {
    if (placed1 = true && HousesUsed < 4) {
        rd.add('placednow', () => available - 5 && HousesUsed + 1)
        
    }
}

function draw() {
    if (placed1 = true && HousesUsed < 5) {
        rd.add('placednow', () => available - 5 && HousesUsed + 1)
        
    }
}